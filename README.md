
  # CMS input

  This is a code bundle for CMS input. The original project is available at https://www.figma.com/design/tf6QLOcN0vwCTgQaNxWsNx/CMS-input.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # cms
